@model List<beah.Domain.Models.Property>

@using (Html.BeginForm("SendMail",
                       "Home",
                       FormMethod.Post))
{
    for (int i = 0; i < Model.Count; i++)
    {
        @Html.HiddenFor(m => m[i].Address.Latitude)
    }
    
}